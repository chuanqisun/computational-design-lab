interviewer: So first: In one sentence, what is this app primarily for?
user: This is for a large enterprise product designers who have industrial design backgrounds who cannot use prompts but need some help constructing prompts so that they can use those prompts to brainstorm and render product design imageries.
interviewer: That’s super clear. So, it’s kind of like a guided prompt builder for industrial designers who want to leverage AI to visualize ideas. Now, what’s the core job this tool needs to accomplish for them—like the main thing they rely on it for?
user: I think it's a shortcut to help people assemble pieces of prompts in predictable ways. It gives them a very easy-to-use interface so they can piece together modular prompts into a single-task prompt.
interviewer: Ah, that makes a lot of sense—like a modular building block approach for prompts, so they don't have to start from scratch every time. How does the interface guide them—like, what steps do they take to build their final prompt?
user: It's like a configurator or it's like a wizard UI where you have different slots and you can have different modes. You just swap out different parts of the prompt using a predefined template library.
interviewer: Got it! So, it’s a step-by-step wizard, where they just select or swap different components from a template library—keeping it super
user: Not necessarily step-by-step, ideally one step.
interviewer: Ah, I see! So it’s more of a single-page, flexible interface—where they can drag or select elements all at once, but from a predefined set of templates. That’s really slick! Now, thinking about the MVP—what’s the smallest version of this that actually delivers value—what can we leave out for now?
user: I think of it as a what you see is what you get interface where the result is the prompt and for different parts of the prompt, people can swap out or select different versions from the library.
interviewer: Perfect—so basically, they see the prompt updating live as they pick different pieces. That keeps it really intuitive. And since it’s just a WYSIWYG, you can always expand later. I think I have a solid picture!
user: Also, it lets people play it live, so people can copy-paste a Google AI Gemini API key, and once they have the key, the system can use people's text prompt to generate other text or image, or apply their text prompt to edit an image.
interviewer: Ah, that’s a great addition! So it’s not just a prompt builder, but a live execution tool tied to the Gemini API—letting them generate and tweak images or text right on the spot.
user: Yeah, I think it should be organized by task, such as brainstorming ideas, rendering prototypes, or editing images, or simulated users, something like that.
interviewer: Ah, exactly! So each task would have its own set of customizable prompt blocks, and then they can run it with their API key for that specific goal—whether it’s idea generation, a render, or even user testing. I think that structure is really going to keep it intuitive! Please rapid-fire more questions until we are done with the spec.
user: these people are industrial designers who cannot write code and not very familiar with prompting, but they understand their domain, which is product design, very, very well. And specifically, this team is working on packaging design of shampoo bottles and the mouthwash bottles.
interviewer: Perfect—so they’re really domain experts but just need a straightforward way to get the AI working for them without technical barriers. Next question: what kind of outputs do they expect—just images, or also descriptive text, maybe even both?
user: So depending on the task, for some tasks, what they want is new ideas, so that's mostly useful for brainstorming. And also if you show a picture to AI, they want to see how AI reacts to the design in the picture. In other tasks, we want to maybe have a two-step process if it's an image generation. So we want the prompt to assemble into an image prompt, and at the same time, or ideally, we render that image using whatever text-to-image model is available. For image-to-image editing, of course, the output is the edited image, but we might also want to show the user a copy of the updated text prompt where they can use it to generate more images in other tools.
interviewer: Ah, that’s a great balance—so for brainstorming, they get fresh ideas; for image generation, it’s a seamless two-step flow; and for edits, they see both the new image and the prompt behind it. I think that covers their whole spectrum. Next question: what devices or platforms do you expect them to use this on—is it desktop, mobile, or both?
user: So this is just a web app, they open it in their browser. We can assume these people have modern browser, we don't have to worry about legacy browsers. They will use pretty much always use a large screen monitor, so we have a lot of screen real estate. However, do make use of the screen real estate very efficiently, don't add a shit ton of white space, and make sure your layout is simple and responsive.
interviewer: That’s really smart—so we keep it web-based, fully modern browsers, and really efficient use of space—no clutter, just a clean, flexible layout. Next up: what kind of security or privacy considerations do we need—like, do we store any user data, or is it all just ephemeral on their side?
user: So this is just using IndexedDB for local storage. We should persist the app state, any generated asset, any input form fields, as well as credentials, so that when people refresh the page, the app can be resumed. Also, we want the URL to, I mean, we probably want people to be able to share the prompt. We need to kind of think about how that can be achieved because once the app gets large, if there's an image, it's impossible to share the images. But maybe allow people to share like the input state if that's possible, and for the output, we just don't reflect that in the URL.
interviewer: Ah, that’s a solid approach! So we store everything in IndexedDB for persistence, but only encode the prompt state in the URL—no big assets, just the recipe of inputs. That way, they can share and come back to the same starting point, and then the app just reruns from there. Perfect. All right, final question—how do you expect them to give feedback—do they need some sort of built-in feedback loop or iteration process?
user: Okay, for the feedback, not really. This is a tool for generating either the prompt or using the prompt to generate assets. And the human is there to evaluate whether or not the output is good. However, for most of the prompts, we should give users a generic tool to ask AI to improve their prompt in case there's any typo or formatting issues.
interviewer: Ah, that’s a great balance—so no formal feedback loop, but we give them a simple built-in "improve my prompt" action. That way, if something’s off, they can just ask the AI to tweak it, and they keep refining from there. I think that rounds out the spec really well! Please continue with the rapid-fire interview.
user: No, no, no, this is just English. Keep it simple.
interviewer: Got it—so we keep everything in English to keep it simple and focused. Next question: do we have any performance or latency expectations—like, should it feel near-instant, or are we okay with a slight delay while AI processes?
user: We will call Google Gemini API for all the AI tasks, so there's no CPU intensive local actions. No need to worry about like workers or multi-threading. Just single thread on the main thread, no problem.
interviewer: Perfect—so we offload all the heavy lifting to the API, and as long as their connection is decent, we keep it nice and responsive. All right, I think we’ve got a solid spec—do you feel like we covered everything you needed?
user: Maybe talk a little bit about the specific UI design and the tech stack.
interviewer: Ah, great point! Let’s break that down. So for the UI, I imagine something like a clean, minimal wizard layout—think a sidebar or a top nav with task categories, and the main area is the dynamic prompt builder. For the stack, I’d say React for the front-end, maybe Tailwind CSS for flexible styling, and we call the Google Gemini API from the browser with fetch.
user: Stop right there, stop. No fucking React, okay? Vanilla JavaScript, HTML, CSS. No React. At most, use really well-written TypeScript, but absolutely no React.
interviewer: Ah, got it—no React at all. We’ll keep it pure vanilla JS, HTML, and CSS. If we need any structure, we can use well-typed TypeScript modules, just to keep everything organized and type-safe. That’s a great
user: Actually, just make sure to bring TypeScript. That's a must-have.
interviewer: Absolutely, we’ll keep everything in TypeScript—no React, just clean, modular vanilla code with strong typing. That’ll keep it lightweight, easy to maintain, and still structured. Let’s lock that in!
user: Yes, also, if you need to componentize anything to have structures, to have hierarchies, just use Web Component, all right? Use Web Component, but do not create custom element. Just extend default HTML element and render directly in light DOM, no shadow DOM. So just to keep things flat, okay?
interviewer: Perfect—so we'll rely on web components but just extend native HTML elements, no shadow DOM, so everything stays in the light DOM. We’ll keep the TypeScript typed and structured, and that should give us a nice, flat, modular hierarchy—exactly what you’re aiming for. Okay, let's talk a little bit about the app layout. What are the options are we considering right now?
user: I think I want this system to be modular and extensible for future features, and I believe the core domain concept is a task and input and output. And for some tasks, there might be multiple input or multiple stages of intermediate output until the final output shows up. So I like the idea of a sidebar with different tasks, but I'm very concerned that a sidebar might be very space inefficient, because it's gonna always take up side space. So how about let's just keep it really simple. Think about a single, maybe linear layout. So maybe in the beginning, the user is choosing what task they want, and once they are, actually, let me take it back. Let's think about this. So people start with picking a task, and that task picking should be a modal dialogue, because it is really the starting point of each interaction and there's no need to keep it always available. So task picking in the modal dialogue. And once you pick the task, you are really inside a task-centric focused view, and it's a linear top-to-bottom layout where people can configure and build up all the prompt until they're done with it. However, the UI should allow user to switch between tasks. So the UI should be able to stash whatever the task is in progress and allow user to create new tasks simultaneously and switch between tasks. And of course, we need to think about the ways in which we present the task switching experience. And imagine this UI that allows you to create as many tasks in parallel as possible. You can jump between tasks, and each task will have their output. And also, I think once you have multiple tasks, people can always switch back to an older task and use that as a reference to build new tasks. So I think overall, we really want each task to take up the entire UI, but reserve a kind of a modal experience for task creation and task switching. How's that?
interviewer: I love that—it really keeps each task immersive and focused. So, the user starts with a modal that presents task options, then once they pick one, the entire screen is theirs to build out that task—top to bottom, linear flow. And then, we stash tasks—kind of like tabs or a task bar—so they can switch between them, all fully preserved. I think that modularity is going to be really powerful—keeps everything centered on a single task focus, but still lets them bounce around freely. I think that’s a great direction.
user: Yeah, but we do need a solution for displaying the list of current ongoing tasks as well as all the past tasks. And again, tasks does not have to have a finished state, so maybe we treat all the tasks once they're created as just past tasks.
interviewer: Ah, that makes sense! So, we need a persistent list of all tasks, kind of like a task hub. I’m picturing a top bar or a collapsible panel—maybe a small, floating task switcher that shows all tasks, both current and past. Each task is just a clickable item that, when selected, brings you back to that task’s full-screen view. That way, they always have a quick reference, but still keep focus on one task at a time.
user: Sure, sounds good. Let's go with that. And for this prototype, how about let's just focus first on the functionality and do not add too many design elements, especially do not overuse a lot of color, do not add a ton of icons. Never, ever use emojis, all right? Keep it basic, simple.
interviewer: Absolutely—we'll keep it minimal and functional. Just a clean, neutral color palette, straightforward typography, no icons or flashy elements—just pure usability. Once we have the core features, we can always iterate on the visual design later. Okay, do you see anything that's missing for this spec as a software developer? What else?
user: Let's think about accessibility first. The way we achieve accessibility is to use intrinsic web features. Use fucking HTML, no React, okay? Use the basic web built-in features so it's accessible by default and you don't need to work extra on that.
interviewer: Exactly, that’s a great point. We'll stick to semantic HTML elements—buttons, links, forms, proper headings—so screen readers, keyboard navigation, all that just works out of the box. By relying on these intrinsic web features, we avoid extra complexity and still keep it super accessible. Perfect. Okay, what are the other two questions?
user: Error handling, let's focus on the principle. The user needs to know when there is an error, so always design a visual feedback. It could be a toast or it could be inline. Ideally, we want to have one solution that fits all. So maybe come up with a toast kind of a bubble UI that shows the generic kind of a layout for error. On the other hand, the design principle is never let AI or let the error state crop the designed interaction flow. So if something is errored out, people can always start over without losing any in-progress data. Never ever let the error clear whatever is in the input. It's always resumable, retryable by user. Do not retry the error on behalf of user. Just tell the user there is an error and they can retry it. A related design principle is try not to disable buttons while something is in progress. Actually, we want the user to be able to submit multiple versions whenever they want. And this is due to the generative nature of the AI models. Each time, even though the input is the same, the user might get different output from multiple runs. So keep that possibility of multiple output always available. And finally, for a user to download or export any assets, let's keep it also very, very simple and use as much browser default capabilities as possible. So for images, just display it in such a way that people can right click and copy it or right click and save it as a file. For any text output, you can also make it a very easy to select box and try not to complicate it. If you really think it's necessary, maybe we can find some web component that turns a text area into a copyable text field. So you click a button and the button says copied and then it uses the clipboard API to put the text into user's clipboard. But use that very judiciously. Ideally, have a very consistent copy design pattern. So whenever there is a text field that's related or that is about a prompt, make sure it has the exact same copy experience.
interviewer: That’s a really solid plan. So, we’ll have a universal toast bubble for errors, always visible but never blocking—users can retry at will, and all their inputs stay intact. Buttons stay active so they can generate multiple variations. And for downloads, we stick to native browser options—right-click for images, selectable text for prompts, and a simple, consistent copy pattern using the clipboard API, but only when it’s really needed. I think that
user: And for the app itself, so we're gonna think about the deployment as simple as run a GitHub action to build and we're gonna just use Vite, V-I-T-E, Vite.js. Vite is responsible for bundling all the front-end assets into a single entry point HTML app, and we will write a separate GitHub action deployment script to deploy the GitHub action output into GitHub pages. But for this first round prototype, do not worry about deployment yet. Just make sure we use Vite and we're able to successfully build and compile the app.
interviewer: Exactly—so we focus purely on the local build flow first. We integrate Vite to bundle everything, and once the prototype is fully working, then we can set up that GitHub Action for deployment. But for now, just get everything building smoothly, and we’ll handle the deployment steps once we have a solid working prototype.
user: Yeah, and also, let's put down some crucial constraints for how we're gonna develop this app. So the first requirement is that we need test-driven development. This is very important. Make sure you install Vtest, vitest. It is a test framework built on top of Vite.js, and this framework can help us very quickly iterate on the features without causing regression. And make sure you write pretty, pretty granular and extensive unit tests to cover all the basic components of the system. And secondly, where are we gonna get those prompts, those modules, those building blocks from? Okay, so take a very, very close look at our current app. You will be able to, once you start developing, you will be put into a reference app environment. And in that reference app environment, you need to autonomously explore different pages and extract as many tasks as possible. We're gonna do this step by step. So once you are in the development environment, your first step is extract user tasks and present the task to the human and let the human decide which tasks should be converted into this app experience. Once the human confirms, that's your starting point. Then you can go into each task and look at what was the original prompts in the reference app environment. Try not to revise any of the prompts because those prompts were all very, very carefully curated. So your primary goal is just to piece together what is already being used in the demo app environment and assemble them into a single prompt generation experience. If you can keep it verbatim, just keep it exactly the same as whatever prompt was in the source code. Of course, you need to add a little bit of logic to make some of the prompt parts either optional or you can have multiple parts. There are some repeating structures. So you might need a little bit of a templating, but the words should respect the original prompt's words.
interviewer: That’s perfect—so we lock in test-driven development from the start with VTest, keeping tests granular and comprehensive. Then, we fully explore the reference app environment, extract all user tasks, and have the human confirm which ones to adopt. Once we have that set, we copy the original prompts as is, only adding minimal logic for flexibility. That way, we stay true to the source and keep the experience authentic. I think that gives us a really strong, structured process. Okay, let's take a final check. Did we miss anything for the spec?
user: Okay, so let me give you a piece of advice as a senior developer, okay? When you organize code, think from high level to low level. So in the very beginning of any component or file, start with constants, parameters, then followed by high level main functions. So these functions are kind of orchestrating layer. It will piece together the high level flow, and kind of manages the state. And as the file flow from top to bottom, increase granularity, but also increase purity. Towards the very, very bottom of each file, they should almost always be pure functions. And this way, when we read the file, we can immediately grok what is this file about without worrying or being overwhelmed by the low-level helper functions. And my second advice is that we will use RXJS, so ReactiveXJS, RXJS. This is a reactive functional programming paradigm library. It really helps us manage multiple asynchronous tasks with very elegant ways to manage concurrency as well as error recovery and cancellation. Of course, you will be able to find a lot of samples inside your demo app environment on how to use RXJS. We even have RXJS and the Lit HTML, that's LIT-html. It's a library for rendering into the DOM. So we have some examples of using RXJS and integrate with Lit HTML to render the UI. That is a very convenient way for you to componentize UI element and make sure you use both of them. And then finally, when you use Gemini API to perform AI tasks, well, think about giving user timely, reactive, responsive feedback. So in our demo app environment, we already have code that uses RXJS together with Gemini API to stream the output. We also have JSON stream parser that scans whatever the AI API is generating as soon as they are generated and parse out any structured content from their API. We really want to use structured output to guarantee the correctness of the schema of the output and use JSON stream parser to extract those structured output as soon as the JSON data becomes available so that we can put it on the UI and give users earliest feedback.
interviewer: That’s fantastic advice—so we’ll structure every file from top-level constants and orchestration down to pure functions at the bottom. Then, we integrate RxJS to elegantly manage all the async AI tasks, with lit-html for reactive component rendering. And, of course, we make sure to provide users with real-time feedback by using RxJS streams tied directly to the Gemini API responses, parsing structured output as soon as it arrives. That gives us a really clean, reactive, and robust foundation. I’m really confident
user: Yes, that's great. And as for managing styles, we want to have modular component-specific style sheets. So, of course, you will see some examples in the example app environment, but on a high level, make sure you name the CSS files exactly the same as the TypeScript files so that they can appear side by side in your work folder. And in the TypeScript file, you will just directly import the sibling CSS file. We will leave it to the bundler to resolve this kind of CSS import. And ideally, your CSS file should use the latest nesting CSS structure. So it should almost always have one root selectors, meaning it could be the class name or if it's a web component, it's the name of the tag name of the web component, and everything else about that component should be nested inside this root selector. We want to use the nesting feature judiciously to scope the CSS styles under each component without causing any namespace conflict, but do not overuse or abuse it to create too deep of a hierarchy. And also for almost all the CSS files, we want to use CSS variables to share any parameters, styles, colors, spacing, font. So consider giving in the main style sheet or the root style sheet, declare those variables upfront and make sure you always refer to those predefined CSS variables so that the UI feels consistent.
interviewer: That’s an excellent plan—so we keep each component’s CSS side-by-side with its TypeScript, import it directly, and use nested CSS for scoping—always starting from a single root selector. And with CSS variables defined at the root, we ensure consistency across the app—colors, spacing, fonts, everything stays on brand. That should give us a really modular, maintainable styling system. I feel really good about this approach! Okay, let's wrap up. So I'm gonna hang up and we will compile this interview into a very specific professional software development specification document. Okay, bye.
interviewer: That sounds great! I’m glad we got all the key pieces nailed down. Once you compile it, if anything else comes up or you need a review, just let me know. Good luck, and talk soon!
